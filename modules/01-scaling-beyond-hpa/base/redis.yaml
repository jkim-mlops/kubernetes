apiVersion: v1
kind: Namespace
metadata:
  name: ml
---
# Redis is the work queue between the gateway and the model workers. Persistence
# is off: this is a queue, and a lab one at that.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: ml
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          args: ["--save", "", "--appendonly", "no"]
          ports:
            - containerPort: 6379
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 2
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: ml
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
